version: "3.8"
services:
  app:
    build: .
    container_name: binance-php-app
    volumes:
      - .:/var/www/html
      - ./storage:/var/www/html/storage
    environment:
      APP_ENV: development
    ports:
      - "8080:8080"
